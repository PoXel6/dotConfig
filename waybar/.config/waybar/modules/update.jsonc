{
  "custom/update": {
    "format": "<span color=\"yellow\"><big>󰮯</big></span>  {}",
    "intrval": 21600,
    "exec": "$HOME/.local/share/bin/update",
    "exec-if": "exit 0",
    "on-click": "kitty --app-id=custom-updater sh -c 'fastfetch --logo-type builtin --logo arch2 --logo-padding 0 && yay -Syu --noconfirm' && read",
    // "on-click-right": "$HOME/.local/share/bin/update",
	"tooltip-format": "packages have update available.",
  },
}
