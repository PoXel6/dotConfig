require("mini.comment").setup({
  options = {
    ignore_blank_line = false,
  },

  mappings = {
    comment = "<C-/>",
    comment_line = "<C-/>",
    comment_visual = "<C-/>",
    textobject = "<C-/>",
  },
})
